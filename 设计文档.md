# 道歉小网站 · 设计文档

> 状态：v3 —— 动画方案已由 SVG 豆豆人更换为 Cartoon/ 目录两帧插画切换（已实现）

## 1. 项目概述

一个部署在 GitHub Pages 上的静态趣味网站，主题是**给宝宝道歉——因为忘记了 TA 的生日**。页面中央是一个道歉小人插画和"道歉按钮"，用户点击后，小人切换为深鞠躬姿势，同时显示俏皮的道歉文案并用浏览器朗读出来；语音播完后小人站回原位。

- **项目代号**：project_apologize
- **使用场景**：把链接发给宝宝，让 TA 点开接受道歉
- **访问方式**：`https://<用户名>.github.io/<仓库名>/`

## 2. 核心功能

| 功能 | 说明 |
|------|------|
| 道歉按钮 | 页面初始为站姿小人（apologize1.png）+ 居中的大按钮（"点我道歉 🙇"） |
| 鞠躬动画 | 两帧切换：点击后切到深鞠躬插画（apologize2.png），语音播完后切回站姿 |
| 道歉文案 | 鞠躬时，小人上方出现气泡文案，从生日道歉文案池中随机选取 |
| 道歉语音 | 用浏览器 SpeechSynthesis 朗读当前文案（首期方案，后续可替换为录音/音频文件） |
| 重复播放 | 语音结束后显示"再道一次歉"按钮，可重复触发 |
| 防连点 | 动画播放期间锁定交互，避免状态错乱 |
| 静音开关 | 页面角落小喇叭图标，偏好存入 `localStorage` |

## 3. 技术选型（已定稿）

- **纯 HTML + CSS + 原生 JavaScript**，零框架、零构建工具
- 小人使用 **Cartoon/ 目录下的两张 PNG 插画**（irasutoya 风格西装小人）：
  - `Cartoon/apologize1.png`：站姿（常态）
  - `Cartoon/apologize2.png`：深鞠躬（道歉态）
  - 通过 `<img>` 切换 `src` 实现两帧动画，无需 CSS 关键帧
- **音效采用方案 A**：浏览器内置 **SpeechSynthesis API** 朗读道歉文案，零素材、零体积
  - 已知限制：音色取决于用户浏览器/系统，且无法精确控制语气
  - **预留替换路径**：语音播放封装为独立函数 `speakApology(text, onEnd)`，后续换成 `.mp3` 录音只需改这一个函数，页面其余逻辑不动
- 部署：直接推送到 GitHub 仓库，在 Settings → Pages 开启 `main` 分支根目录即可，无需 CI

## 4. 页面结构

```
┌─────────────────────────────┐
│   给 {宝宝名字} 的道歉专用页  │  ← 标题，名字用占位符
│                             │
│          🧍（站姿小人）       │  ← 初始状态：apologize1.png
│      ┌───────────────┐      │
│      │  点我道歉 🙇   │      │
│      └───────────────┘      │
│                          🔊 │  ← 静音开关（右下角）
└─────────────────────────────┘

点击后：
┌─────────────────────────────┐
│      ╭──────────────────╮   │
│      │ 宝宝对不起！我把  │   │  ← 文案气泡（随机）
│      │ 你生日忘光了……    │   │
│      ╰──────┬───────────╯   │
│          🙇（深鞠躬）        │  ← apologize2.png，持续到语音播完
└─────────────────────────────┘
```

### 视觉风格

- **小人形象**：irasutoya 风格西装小人插画（站姿 / 深鞠躬两帧）
- 配色：暖色系 —— 淡粉/奶油色渐变背景，粉色系按钮与气泡描边
- 文案气泡：圆角白底，带小三角指向小人头部，浮现时带轻微弹跳
- 标题：「给 {宝宝名字} 的道歉专用页」，`{宝宝名字}` 为占位符，部署前替换
- 整体基调：俏皮可爱、诚意满满

## 5. 动画设计（两帧切换）

交互时间线（时长由语音决定）：

| 时机 | 动作 |
|------|------|
| 点击按钮 | 按钮隐藏，`img.src` 切到 apologize2.png（预加载，无闪烁），气泡弹出随机文案，同时开始朗读 |
| 语音播放中 | 保持鞠躬姿势 |
| 语音播完（`utterance.onend`） | `img.src` 切回 apologize1.png，显示"再道一次歉"按钮 |

实现要点：

- 页面加载时预加载 apologize2.png（`new Image().src`），避免首次点击时白闪
- **结束时机以 `utterance.onend` / `onerror` 为准**，与语音严格同步
- 兜底逻辑：
  - 静音或浏览器不支持语音时，按文案长度估算鞠躬时长（`max(2s, 字数 × 320ms)`）
  - 播放中点静音时，部分浏览器 `cancel()` 不触发 `onend`，额外用 400ms 定时器兜底收尾
- `finishApology()` 用 `playing` 标志保证幂等，重复触发安全

## 6. 音效设计（已定稿）

- 首期使用 **SpeechSynthesis**：气泡文案出现时调用 `speakApology(text, onEnd)` 朗读当前文案
  - 语速稍慢（`rate ≈ 0.9`）、音调稍高（`pitch ≈ 1.2`），尽量贴近委屈巴巴的感觉
  - 优先选择中文语音包（`lang: 'zh-CN'`）
  - 自动播放策略：必须在用户点击事件回调内首次触发——本设计由按钮点击发起，天然满足
- 静音开关：点击切换，`speechSynthesis.cancel()` 立即停止当前朗读，偏好存 `localStorage`
- 后续替换为录音时：仅需将 `speakApology` 内部改为播放对应音频文件，`onEnd` 改挂 `audio.onended`

## 7. 文案池

主题：俏皮地围绕"宝宝，对不起忘记了你的生日"。每次点击随机选取一条：

1. 「宝宝对不起！我把你的生日忘得一干二净……」
2. 「我错了！忘记你的生日，先给您鞠一躬！」
3. 「连宝宝的生日都能忘，我这就把自己打包寄走」
4. 「对不起宝宝，我的脑子请假了，没带上我」
5. 「我错了宝宝，请宝宝从重发落（但别不理我）」
6. 「宝宝消消气，我忏悔，我面壁，我请吃大餐！」
7. 「全世界最坏的人就是我，居然忘了宝宝的生日！」
8. 「这一躬鞠给全世界最好的宝宝，原谅我这一次嘛～」

- 文案以 JS 数组维护，后续增删无需改动逻辑
- 同一条文案连续出现的概率做简单规避（记录上一条索引，随机到相同则重取一次）
- 语音朗读的就是当前显示的文案，视听一致

## 8. 文件结构

```
project_apologize/
├── index.html            # 页面结构 + <img> 小人
├── style.css             # 布局、按钮与气泡样式、图片尺寸
├── main.js               # 两帧切换、文案池、speakApology()、状态锁
├── Cartoon/
│   ├── apologize1.png    # 站姿（常态）
│   └── apologize2.png    # 深鞠躬（道歉态）
├── 设计文档.md            # 本文档
└── README.md             # 项目说明 + 部署说明
```

## 9. 部署方案（GitHub Pages）

1. 本地完成开发，推送到 GitHub 仓库 `project_apologize`
2. 仓库 Settings → Pages → Source 选 `main` 分支 `/ (root)`
3. 等待约 1 分钟，访问 `https://<用户名>.github.io/project_apologize/`
4. 无需任何构建步骤，后续修改直接 push 即生效
5. 上线前记得把标题里的 `{宝宝名字}` 占位符替换为真实昵称

## 10. 浏览器兼容

- 目标：现代浏览器（Chrome / Edge / Firefox / Safari 近两年版本）
- 使用特性均为广泛支持：PNG、CSS Animation（仅气泡弹出）、SpeechSynthesis（Safari 中文音色略有差异，可接受）
- 移动端适配：图片高度用 `min(52vh, 380px)`，viewport 设置，竖屏可正常交互——宝宝大概率是手机点开链接

## 11. 实施步骤

1. ~~搭建 `index.html` / `style.css` / `main.js` 骨架~~ ✅
2. ~~SVG 豆豆人鞠躬动画~~ → v3 已改为 Cartoon/ 两帧插画切换 ✅
3. ~~接入文案池与气泡显示（含防连续重复）~~ ✅
4. ~~接入 `speakApology()` 语音 + 静音开关 + 语音结束同步~~ ✅
5. ~~防连点状态机、重复播放闭环~~ ✅
6. 待办：推送 GitHub → 开启 Pages → 线上验证

## 12. 已定稿事项记录

| 事项 | 结论 |
|------|------|
| 动画形式 | ~~CSS/SVG 手绘动画~~ → v3：Cartoon/ 两帧 PNG 插画切换，结束时机与语音同步 |
| 触发方式 | 只点击特定按钮 |
| 音效方案 | 方案 A：浏览器 SpeechSynthesis 朗读，预留替换为音频文件的接口 |
| 文案风格 | 俏皮，围绕"忘记宝宝生日"主题，8 条已定稿 |
| 页面标题 | 「给 {宝宝名字} 的道歉专用页」，占位符部署前替换 |
| 小人形象 | ~~豆豆人~~ → v3：irasutoya 风格西装小人插画（站姿 + 深鞠躬） |
| 技术栈 | 纯 HTML/CSS/JS，零依赖 |
